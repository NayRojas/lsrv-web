import { ResultsPresenterTs } from './results.presenter';

describe('ResultsPresenterTs', () => {
  it('should create an instance', () => {
    expect(new ResultsPresenterTs()).toBeTruthy();
  });
});
