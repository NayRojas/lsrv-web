import { StatisticsPresenterTs } from './statistics.presenter';

describe('StatisticsPresenterTs', () => {
  it('should create an instance', () => {
    expect(new StatisticsPresenterTs()).toBeTruthy();
  });
});
